server {
    listen         80 default_server;
    listen         [::]:80 default_server;
    # server_name    react-dashboard react-dashboard;
    root           /var/www/;
    index          index.html;
    try_files $uri /index.html;
}